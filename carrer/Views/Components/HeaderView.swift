import SwiftUI

struct HeaderView: View {
    var body: some View {
        HStack {
            Text("Career Guidance Dashboard")
                .font(.largeTitle)
                .fontWeight(.bold)
                .padding(.top)
            Spacer()
        }
    }
}